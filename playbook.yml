- name: Installation HTTPD
  hosts: all
  become: true
  tasks:
    - name: packages install
      dnf:
        name:
          - httpd
        state: latest
    - name: Ensure httpd service is running
      systemd:
        name: mysqld
        state: restarted
        enabled: yes

   
